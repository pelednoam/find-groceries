{
  "name": "find-groceries-site",
  "private": true,
  "version": "1.0.0",
  "description": "Static site for find-groceries. TypeScript, compiled to docs/app.js.",
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "smoke": "node tests/site_smoke.js",
    "check": "npm run typecheck && npm run build && npm run smoke"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/leaflet": "^1.9.12",
    "@types/node": "^22.10.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2"
  }
}
